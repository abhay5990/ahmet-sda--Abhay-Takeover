"""
Proxy provider clients.

These are API clients for external proxy services (Proxyline, DataImpulse).
They fetch proxy lists and manage proxy subscriptions.

Distinct from infrastructure/proxy/ which handles runtime pool/rotation.
"""
